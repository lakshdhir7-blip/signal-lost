# Dev uses the Vite proxy in vite.config.ts; leaving the API URL unset so the
# code falls back to the relative `/api/hint` path that the proxy forwards
# to the local worker on http://localhost:8787.
